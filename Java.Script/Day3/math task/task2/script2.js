

do { 
    var userInput = prompt ("Please, Enter another value"); 
    var value = Number(userInput);
}
while (isNaN(userInput))
    var square = Math.sqrt (value);
alert ("The square route is " + square)